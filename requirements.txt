# core (needed everywhere, including on the UNO Q Linux side)
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7
opencv-python-headless>=4.8

# laptop phase only (days 1-7): audio capture from the USB sound card
sounddevice>=0.4.6

# hardware link (days 8+): serial protocol to the MCU
pyserial>=3.5
